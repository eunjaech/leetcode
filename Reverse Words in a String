class Solution:
    def reverseWords(self, s: str) -> str:
        s = s.split()
        i = 0
        res = ""
        while i < len(s):
            res += s[len(s)-1-i]
            if (i < len(s)-1):
                res += " "
            i +=1

        return res
